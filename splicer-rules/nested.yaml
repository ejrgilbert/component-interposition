version: 1

# This creates a composition that has a nested node in the service chain
# (gets run with the chain.yaml composition)
rules:
  - before:
      interface: "wasi:http/handler@0.3.0-rc-2026-01-06"
      provider:
        name: "srv-a"
    inject:
      - name: srv-c
        path: "./fixtures/service_c.comp.wasm"
