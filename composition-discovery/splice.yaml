version: 1

rules:
  - match_on:
      interface: wasi:http/handler@0.3.0-rc-2026-01-06
      provider_name: srv-b
    between:
      inner: srv-b
      outer: srv
    middlewares:
      - middleware-a
      - middleware-b
